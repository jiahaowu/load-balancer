#!/bin/bash

./cleanme
unamestr=`uname`

if [[ "$unamestr" == 'Linux' ]]; then
   gcc -fopenmp -pg -o benchmark.exe benchmark.c -lm
elif [[ "$unamestr" == 'Darwin' ]]; then
   gcc-6 -fopenmp -pg -o benchmark.exe benchmark.c -lm
fi
 